{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port=1500",
    "generate": "nuxt generate",
    "preview": "cross-env PORT=1500 node .output/server/index.mjs",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "imask": "^6.4.3",
    "nuxt": "3.0.0-rc.11",
    "sass": "^1.55.0",
    "sass-loader": "^13.1.0",
    "swiper": "^8.4.3"
  }
}
